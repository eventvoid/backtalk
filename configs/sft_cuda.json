{
  "data_dir": "data/prepared/backtalk-sft-ctx2048",
  "tokenizer": "tokenizers/backtalk-tokenizer/tokenizer.json",
  "base_checkpoint": "checkpoints/backtalk-base/ckpt_best.pt",
  "out_dir": "checkpoints/backtalk-sft",
  "block_size": 2048,
  "batch_size": 64,
  "gradient_accumulation_steps": 1,
  "max_steps": 4000,
  "lr": 0.00002,
  "min_lr": 0.000002,
  "weight_decay": 0.01,
  "beta1": 0.9,
  "beta2": 0.95,
  "grad_clip": 1.0,
  "warmup_steps": 100,
  "lr_decay_steps": 4000,
  "eval_interval": 100,
  "eval_batches": 20,
  "log_interval": 10,
  "early_stopping_patience": 5,
  "early_stopping_min_delta": 0.005,
  "seed": 1337,
  "device": "cuda",
  "precision": "bf16"
}
